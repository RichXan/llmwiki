---
title: AGENTS
type: meta
aliases:
  - 工作规范
  - Agent 规范
tags:
  - meta
  - agent
  - 规范
created: 2026-08-17
updated: 2026-08-17
---

# AGENTS.md — 本库的长期维护规范

本文件是本知识库（LLM Wiki）的**最高维护规范**。任何自动化或人工维护本库的 Agent，在处理资料、创建页面、更新内容之前，都必须先阅读并遵守本文件。

## 一、三层结构

本库采用三层结构，各层职责明确，互不越界：

| 层级 | 目录 | 职责 | 权限 |
| ---- | ---- | ---- | ---- |
| **Raw 层** | `raw/` | 保存原始资料，是事实的唯一来源（Source of Truth） | **只读**，禁止改写、删除、移动 |
| **Wiki 层** | `wiki/` | 保存整理后的结构化知识，由 Agent 增量维护 | 可读可写（遵循本规范） |
| **Schema 层** | 根目录 `AGENTS.md` | 定义归档、更新、引用、冲突处理规则 | 人工审慎修改 |

```
raw/
  articles/   文章与网页剪藏
  papers/     论文与报告
  books/      书籍、划线与读书笔记
  chats/      有价值的 AI 对话
  notes/      灵感与随手记录
  meetings/   会议纪要与转写

wiki/
  sources/    raw/ 中每份资料的来源摘要页
  concepts/   概念、理论、方法页
  entities/   人物、公司、产品、工具页
  topics/     跨来源的主题综述页
```

## 二、核心维护规则（必须遵守）

1. **先搜后建**：处理新资料前，先搜索 `wiki/` 中已有的相关页面，判断是「补充旧页面」还是「创建新页面」。能归并到现有页面的，不新建重复页面。

2. **Raw 只读**：`raw/` 是事实来源，**禁止改写、删除或移动其中的任何文件**。对原文的任何加工（摘要、提炼、结构化）都只能发生在 `wiki/` 层。

3. **一源一页**：每份 Raw 资料在 `wiki/sources/` 中建立对应的来源摘要页，并通过 `[[相对路径]]` 保留对原始文件的链接。原始文件链接不可省略。

4. **分页建链**：概念、实体、主题各自使用独立页面，通过 Obsidian 双链 `[[...]]` 建立关系。避免把所有信息堆在一篇笔记中；一次只讲清一个对象。

5. **事实必溯源**：所有事实性内容都必须标注来源（链接到 `wiki/sources/` 对应页或原始文件）。无法确认的内容，明确标注为「**待核实**」，不得臆测或当作事实陈述。

6. **分歧并存**：新旧资料出现分歧时，同时保留不同说法，并标注各自的**来源、时间、适用范围**，不直接覆盖旧结论。读者自行判断，Agent 不替用户做取舍。

7. **增量维护**：每次只更新受影响的页面，并同步维护相关双链、`index.md` 和 `log.md`。不因一次更新而大规模重构无关页面。

8. **只追加日志**：`log.md` 采用只追加方式（append-only），按时间顺序记录：日期、资料来源、新建页面、更新页面、待人工确认事项。**不修改、不删除历史记录**。

9. **不擅删文件**：不擅自删除现有文件。遇到同名文件已存在时，先读取原内容，合并必要规则，**保留原有内容**，只做增量补充。

10. **存疑即停**：当信息不足、或判断可能影响整体结构（如目录调整、页面合并/拆分、层级变更）时，**暂停执行并先向用户提问**，得到确认后再继续。

## 三、命名与格式约定

- 文件名：使用小写连字符命名，如 `transformers-架构.md`；来源页用资料标题或日期+关键词，如 `2026-08-17-attention-is-all-you-need.md`。
- 页面属性：每个 Wiki 页面顶部使用 Obsidian YAML 属性（frontmatter），至少包含 `title`、`type`、`aliases`、`tags`、`sources`、`created`、`updated`。
- 双链：引用其他页面用 `[[页面名]]`；引用 raw 原文件用 `[[raw/...路径/文件名]]`（不含扩展名时需保证唯一）。
- 别名：同一对象有多个叫法时写入 `aliases`，便于搜索与双链命中。

## 四、处理新资料的标准流程

1. 把资料放入 `raw/` 下对应分类目录（不改动原内容）。
2. 搜索 `wiki/`，确认是否已有相关 concept / entity / topic / source 页面。
3. 在 `wiki/sources/` 为该资料创建或更新来源摘要页（含原始文件链接）。
4. 提炼知识，写入或更新对应的 concept / entity / topic 页面，并建立双链。
5. 标注来源；无法确认的内容写「待核实」。
6. 更新 `index.md`（新增条目）与 `log.md`（追加记录）。

## 五、页面模板

各目录下的 `_template.md` 提供标准骨架。新建页面时复制对应模板，再填写内容：

- `wiki/sources/_template.md` — 来源摘要页模板
- `wiki/concepts/_template.md` — 概念/理论/方法页模板
- `wiki/entities/_template.md` — 人物/公司/产品/工具页模板
- `wiki/topics/_template.md` — 主题综述页模板

模板文件本身是占位说明，不应被当作正式知识页引用。
